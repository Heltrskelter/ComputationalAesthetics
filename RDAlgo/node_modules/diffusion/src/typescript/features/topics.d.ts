import {Subscription} from './../events/subscription.d';
import {View} from './../events/view.d';
import {FetchStream} from './../events/fetch-stream.d';
import {Result} from './../events/result.d';
import {Json} from '../data/json.d';
import {Binary} from '../data/binary.d';

export interface Topics {
  subscribe: (selector: string, callback?) => Subscription;
  unsubscribe: (selector: string) => void;
  stream: (selector: string, callback?) => Subscription;
  view: (selector: string, callback?) => View;
  fetch: (selector: string) => FetchStream;
}

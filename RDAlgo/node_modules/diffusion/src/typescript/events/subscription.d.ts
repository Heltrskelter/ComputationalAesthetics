import {TypedSubscription} from './typed-subscription.d';
import {DataType} from '../data/datatype.d';
import {View} from './view.d';
import {Stream} from './stream.d'
import {Binary} from '../data/binary.d';
import {Json} from '../data/json.d';

export interface Subscription extends Stream {
  selector: string;
  asType: (type: DataType<Binary> | DataType<Json>) => TypedSubscription;
  view: View;
  transform: (transformer) => Subscription;
}

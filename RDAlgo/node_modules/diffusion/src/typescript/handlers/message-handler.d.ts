import {SessionMessage} from '../features/messages.d';

export interface MessageHandler {
  onMessage: (message: SessionMessage) => void;
  onActive: (unregister: any) => void;
  onClose: () => void;
}

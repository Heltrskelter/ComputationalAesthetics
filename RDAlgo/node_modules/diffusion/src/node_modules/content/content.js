var RecordContent = require('content/record-content/record-content');
var RecordContentBuilder = require('content/record-content/builder');

var StructuredRecordContent = require('content/structured-record-content/record-content');
var StructuredRecordContentBuilder = require('content/structured-record-content/builder');

RecordContent.Builder = RecordContentBuilder;
StructuredRecordContent.Builder = StructuredRecordContentBuilder;

module.exports = {
    RecordContent : RecordContent,
    StructuredRecordContent : StructuredRecordContent
};